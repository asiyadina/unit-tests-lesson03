package seminars.third.tdd;

public class MoodAnalyser {
    public String analyser(String str){
        if (srt.contains("хорошее"))
            return "Happy";
        if (srt.contains("плохое"))
            return "sad";
        return "so so";
    }
}
